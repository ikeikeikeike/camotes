GET     /admins/proxy                                        presentation.controllers.Admins.proxy
GET     /admins/request                                      presentation.controllers.Admins.requestForm
POST    /admins/request                                      presentation.controllers.Admins.request

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                                        controllers.Assets.versioned(path="/public", file: Asset)
