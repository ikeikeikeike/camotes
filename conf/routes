GET     /proxy                                        presentation.controllers.Pages.proxy
GET     /request                                      presentation.controllers.Pages.requestForm
POST    /request                                      presentation.controllers.Pages.request

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                                 controllers.Assets.versioned(path="/public", file: Asset)
